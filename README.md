# Locations
.vimrc = C:\Users\{your_name}\.vimrc

dict/ = D:\w64devkit\bin\vim-home\dict
# installation
unzip vim.zip and add the folder to PATH
then set .vmrc & dict/ as given.
Then type `vim` in cmd/other shell to run.
VIM use maximum 20MB RAM!!
>I use *w64devkit*"s buitin vim so my setup is somewhat weired.You need to esit .vmrc fo custom location.The file has nice commments to guide yu.
>Enjoy VIM!!!!!
